# Live mode. Leave unset to run the whole fabric in deterministic dry-run.
# `ant auth login` works too — the SDK picks up the profile with no env var.
ANTHROPIC_API_KEY=

# Optional: point at a different config directory
DECISION_FABRIC_CONFIG=./config
